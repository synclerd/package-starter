#!/usr/bin/env node
import { Environment } from './types';
export declare const testEnv: Environment;
export { Environment, App, Package, Provider, ProviderMetadata, Source, Movie, Show, Season, Episode, SourceTypes, PackageManifest, } from './types';
export { HttpClientFactory, HttpRequestHeaders, HttpResponseHeaders, HttpTransformer, HttpAdapter, HttpBasicCredentials, HttpProxyConfig, Method, ResponseType, TransitionalOptions, HttpRequestConfig, HttpResponse, HttpError, HttpPromise, CancelStatic, Canceler, CancelTokenStatic, CancelToken, CancelTokenSource, HttpInterceptorManager, HttpClient, HttpClientInstance } from './types';
export { Storage } from './types';
